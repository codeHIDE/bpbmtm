package com.bypay.dao;

import java.util.List;
import java.util.Map;
import com.bypay.domain.TerminalType;

public interface TerminalTypeDao {
	List<TerminalType> selectTerminalTypeAll();
	
	//添加对应表
	int insertTerminalType(TerminalType terminalType);
	
	
	//查询对应码表
	List<TerminalType> selectTerminalTypeList(Map map);
	
	
	//查询对应码表的条数 分页用
	int selectTerminalTypeListCount( TerminalType terminalType);
	
	//删除样式
	int deleteTerminalType(TerminalType terminalType);

}
