package com.bypay.dao;

import com.bypay.domain.MerManager;

public interface MerManagerDao {
	Integer selectMerManagerNameByMid(MerManager merManager);
	
	MerManager selectMerManagerRemarkByMid(MerManager merManager);
	
	Integer insertMerManager(MerManager merManager);
	
	Integer updateMerManager(MerManager merManager);
	
	 /**
	   * 
	   * @Description: 重置机构商密码
	   * @param AgenctId
	   * @return
	   */
	  Integer updateMerManagerPassword(MerManager merManager);
}
